class Network:
    def __init__(self, size_vertices, size_edges, demand, adjacent_matrix, weight, cappacity):
        self.size_vertices = size_vertices
        self.size_edges = size_edges
        self.demand = demand
        self.adjacent_matrix = adjacent_matrix
        self.weight = weight
        self.cappacity = cappacity

    
